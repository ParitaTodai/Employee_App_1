import { EmpModifyPipe } from './emp-modify.pipe';

describe('EmpModifyPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpModifyPipe();
    expect(pipe).toBeTruthy();
  });
});
